name: api 1
author: gary
collections:
  - name: Invoice Gateway
    requests:
      - name: Invoice Headers
        method: GET
        url: |
          https://www.reece.com.au/site/menu
        headers:
          content-type: application/json
          consumer-key: 12345
